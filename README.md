# 🤖 Discord Bot — Полная документация

## 📁 Структура проекта

```
discord_bot/
├── main.py                  # Точка входа, запуск бота
├── config.py                # ВСЕ настройки (токены, ID каналов, ролей, кулдауны)
├── database.py              # JSON-хранилище данных (кулдауны, авто-ответы и т.д.)
├── requirements.txt         # Зависимости
├── data.json                # Создаётся автоматически при первом запуске
│
├── utils/
│   ├── helpers.py           # Вспомогательные функции (транскрипты, парсинг времени)
│   └── checks.py            # Система проверки прав доступа
│
└── cogs/
    ├── tickets.py           # 📩 Система тикетов
    ├── applications.py      # 📋 Заявки в команду
    ├── moderation.py        # 🔨 Модерация (mute/ban/kick + авто-разбан)
    ├── auto_reply.py        # 🤖 Авто-ответ на сообщения игроков
    ├── donate.py            # 💳 Магазин доната (/dotane)
    ├── info_cmd.py          # ℹ️ Команда /info
    ├── ai_chat.py           # 🧠 ИИ чат (Anthropic Claude)
    ├── access_control.py    # 🔐 Управление доступом к командам
    └── admin_utils.py       # 🛠️ Утилиты (/say, /say-dm, /ip)
```

---

## ⚙️ Установка и запуск

### 1. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 2. Настройка config.py

Открой `config.py` и заполни:

| Параметр | Описание |
|----------|----------|
| `TOKEN` | Токен бота с Discord Developer Portal |
| `AI_API_KEY` | Ключ Anthropic API (для ИИ чата, необязательно) |
| `ADMIN_ROLE_ID` | ID роли администратора |
| `SUPPORT_ROLE_ID` | ID роли поддержки |
| `TECH_SUPPORT_ROLE_ID` | ID роли тех-поддержки |
| `STAFF_PING_ROLES` | Список ID ролей, которых пинговать при заявке в персонал |
| `DS_ADMIN_PING_ROLES` | Список ID ролей для заявок в дс-адм |
| `BUILDER_PING_ROLES` | Список ID ролей для заявок в билдеры |
| `SUPPORT_PING_ROLES` | Список ID ролей для тикетов поддержки |
| `TECH_SUPPORT_PING_ROLES` | Список ID ролей для тикетов тех-поддержки |
| `LOG_CHANNEL_1_ID` | Главный канал логов |
| `LOG_CHANNEL_2_ID` | Второй канал логов |
| `APP_CH_STAFF` | Канал для заявок в персонал |
| `APP_CH_DS_ADMIN` | Канал для заявок в дс-адм |
| `APP_CH_BUILDER` | Канал для заявок в билдеры |
| `AI_CHANNEL_ID` | Канал для ИИ чата (или настраивается через /ии канал) |
| `TICKET_CATEGORY_ID` | Категория для тикетов (None = без категории) |
| `TICKET_COOLDOWN` | Кулдаун тикетов в секундах (по умолчанию 300 = 5 мин) |
| `APPLICATION_COOLDOWN` | Кулдаун заявок в секундах (по умолчанию 86400 = 24 ч) |

### 3. Запуск
```bash
python main.py
```

---

## 📋 Список всех команд

### 📩 Тикеты
| Команда | Описание |
|---------|----------|
| `/setup-tickets` | Установить панель тикетов в канал |
| `/ticket add @игрок` | Добавить игрока в тикет |
| `/ticket remove @игрок` | Убрать игрока из тикета |
| `/ticket rename имя` | Переименовать канал тикета |

**Типы тикетов:**
- 💬 **Поддержка** — общие вопросы, жалобы
- ⚙️ **Тех-поддержка** — технические проблемы

**Название канала:** `поддержка-ник` / `техподдержка-ник`

---

### 📋 Заявки в команду
| Команда | Описание |
|---------|----------|
| `/setup-applications` | Установить панель заявок |

**Направления:** Персонал / ДС-Адм / Билдеры

**При отклонении:** модератор указывает причину → отображается в канале с заявкой И отправляется в ЛС игроку.

---

### 🔨 Модерация
| Команда | Описание |
|---------|----------|
| `/ds-panel @игрок` | Открыть панель модерации |
| `/purge число` | Удалить до 1000 сообщений (bulk delete) |

**Действия в панели:** Mute / Ban (временный, с авто-разбаном) / Kick / Снять Mute

**Формат времени:** `30s`, `10m`, `2h`, `7d`

---

### 🤖 Авто-ответ
| Команда | Описание |
|---------|----------|
| `/авто-ответ @игрок текст1 [текст2] ... [текст5]` | Установить авто-ответ (до 5 вариантов) |
| `/авто-ответ-выкл @игрок` | Отключить авто-ответ |
| `/авто-ответ-список` | Список активных авто-ответов |

Бот отвечает **случайным** из заданных вариантов на каждое сообщение игрока.

---

### 💳 Магазин доната
| Команда | Описание |
|---------|----------|
| `/dotane` | Показать товары и способы оплаты |
| `/donate-admin текст` | Изменить текст магазина |
| `/donate-admin товар-добавить название цена [описание]` | Добавить товар |
| `/donate-admin товар-удалить номер` | Удалить товар |
| `/donate-admin оплата-добавить название реквизиты` | Добавить способ оплаты |
| `/donate-admin оплата-удалить номер` | Удалить способ оплаты |
| `/donate-admin список` | Посмотреть текущий список |

---

### ℹ️ Информация о сервере
| Команда | Описание |
|---------|----------|
| `/info` | Получить информацию в ЛС |
| `/info-настройка текст` | Изменить текст |
| `/info-настройка ссылка-добавить название ссылка` | Добавить ссылку |
| `/info-настройка ссылка-удалить номер` | Удалить ссылку |
| `/info-настройка показать` | Предпросмотр |

---

### 🧠 ИИ Чат (Anthropic Claude)
| Команда | Описание |
|---------|----------|
| `/ии канал` | Установить текущий канал как ИИ-чат |
| `/ии выкл` | Отключить ИИ-чат |
| `/ии сброс` | Сбросить историю разговора |

Бот играет роль **Гоблина** — дерзкого и остроумного персонажа, который подшучивает над игроками.

**Для включения:** укажи `AI_API_KEY` в `config.py` (ключ с console.anthropic.com).

---

### 🔐 Система доступа к командам
| Команда | Описание |
|---------|----------|
| `/доступ добавить команда @роль/@пользователь` | Выдать доступ |
| `/доступ убрать команда @роль/@пользователь` | Забрать доступ |
| `/доступ список команда` | Кто имеет доступ |
| `/доступ очистить команда` | Сбросить все права |
| `/доступ все-команды` | Все настроенные команды |

**Администраторы сервера имеют доступ ко всему автоматически.**

Команды с кастомным доступом: `ds-panel`, `purge`, `say`, `say-dm`, `авто-ответ`, `авто-ответ-выкл` и другие.

---

### 🛠️ Утилиты
| Команда | Описание |
|---------|----------|
| `/say текст [#канал]` | Написать от имени бота |
| `/say-dm @игрок текст` | Написать игроку в ЛС от имени бота |
| `/ip ник` | Информация об игроке (требует настройки MySQL) |

---

## 🔧 Настройка /ip (MySQL)

1. Установи: `pip install aiomysql`
2. В `cogs/admin_utils.py` заполни `MYSQL_CONFIG`
3. Раскомментируй блок с запросом
4. Адаптируй SQL-запрос под свою БД (AuthMe / CoreProtect / LiteBans / и т.д.)

---

## ❗ Требуемые права бота в Discord

- `Send Messages` + `Embed Links` + `Attach Files`
- `Manage Channels` (создание/удаление каналов тикетов)
- `Manage Messages` (purge)
- `Moderate Members` (mute/timeout)
- `Ban Members`
- `Kick Members`
- `Read Message History`

---

## 📝 Заметки

- `data.json` создаётся автоматически и хранит: кулдауны, авто-ответы, временные баны, настройки доната/инфо, права доступа, ID ИИ-канала.
- При перезапуске бота все персистентные кнопки (тикеты, заявки) продолжают работать.
- Авто-разбан проверяется каждую минуту фоновым тасом.
